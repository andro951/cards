diff --git a/site/deck.js b/site/deck.js
index b820964..4ae6467 100644
--- a/site/deck.js
+++ b/site/deck.js
@@ -4,0 +5 @@
+import {creditFields,bindCreditFields} from './credits.js';
@@ -36 +37 @@
-    <div class="tabs"><button data-tab="cards" class="${tab==='cards'?'active':''}">Cards <span>${summary.cards}</span></button><button data-tab="setup" class="${tab==='setup'?'active':''}">Art & setup</button><button data-tab="review" class="${tab==='review'?'active':''}">Review <span>${summary.warnings+summary.errors||''}</span></button></div><div id="deck-body"></div>`;
+    <div class="tabs"><button data-tab="cards" class="${tab==='cards'?'active':''}">Cards <span>${summary.cards}</span></button><button data-tab="setup" class="${tab==='setup'?'active':''}">Art & setup</button><button data-tab="review" class="${tab==='review'?'active':''}">Review <span>${summary.warnings+summary.errors||''}</span></button></div>${d.upgradeRequired?'<div class="notice info">This deck was prepared with the previous Card Tools version. Generate images to apply v54 and the new artist-credit rules. Saved order ZIPs are unchanged.</div>':''}<div id="deck-body"></div>`;
@@ -65 +66 @@
-      return `<article class="card-item"><button class="card-image" data-card="${c.id}" aria-label="Edit ${esc(c.name)}"><span class="quantity-pill">${c.quantity}×</span>${warning?'<span class="warn-pill">!</span>':''}${url?`<img src="${esc(url)}" class="${done?'':'art-only'}" loading="lazy" alt="${esc(c.name)}${flipped?' back':''}">`:'<span class="card-empty-symbol">▱</span>'}${!done?`<span class="image-label">${flipped?'No back selected':f.error?'Needs attention':'Art preview · not rendered'}</span>`:dirty?'<span class="image-label">Previous render · changes pending</span>':''}</button><h3 title="${esc(c.name)}">${esc(c.name)}</h3><div class="card-item-footer"><small>${esc((c.scryfall.set||'').toUpperCase())} ${esc(c.scryfall.collector_number||'')} · ${esc(flipped?'Back':f.compiled?.render?'Rendered':'Not rendered')}</small><button class="flip-button" data-flip="${c.id}" aria-label="Show ${flipped?'front':'back'} of ${esc(c.name)}">${flipped?'Front':'Back'} ↻</button></div></article>`;
+      return `<article class="card-item"><button class="card-image" data-card="${c.id}" aria-label="Edit ${esc(c.name)}"><span class="quantity-pill">${c.quantity}×</span>${warning?'<span class="warn-pill">!</span>':''}${url?`<img src="${esc(url)}" class="${done?'':'art-only'}" loading="lazy" alt="${esc(c.name)}${flipped?' back':''}">`:'<span class="card-empty-symbol">▱</span>'}${!done?`<span class="image-label">${flipped?'No back selected':f.error?'Needs attention':'Art preview · not rendered'}</span>`:dirty?'<span class="image-label">Previous render · changes pending</span>':''}</button><h3 title="${esc(c.name)}">${esc(c.name)}</h3>${f.compiled?.credit?`<div class="card-credit" title="${esc(f.compiled.credit.display)}">${esc(f.compiled.credit.display)}</div>`:''}<div class="card-item-footer"><small>${esc((c.scryfall.set||'').toUpperCase())} ${esc(c.scryfall.collector_number||'')} · ${esc(flipped?'Back':f.compiled?.render?'Rendered':'Not rendered')}</small><button class="flip-button" data-flip="${c.id}" aria-label="Show ${flipped?'front':'back'} of ${esc(c.name)}">${flipped?'Front':'Back'} ↻</button></div></article>`;
@@ -86 +87 @@
-  $('#use-as-defaults').onclick=()=>attempt(async()=>{const defaults={symbols:d.settings.symbols,backAsset:d.settings.backAsset,artist:d.settings.artist,templateRules:d.settings.templateRules};await api('/api/settings',{defaults});closeModal();toast('Symbols, back, artist and templates saved as defaults for new decks.');});
+  $('#use-as-defaults').onclick=()=>attempt(async()=>{const defaults={symbols:d.settings.symbols,backAsset:d.settings.backAsset,artist:d.settings.artist,modificationCredit:d.settings.modificationCredit||'',templateRules:d.settings.templateRules};await api('/api/settings',{defaults});closeModal();toast('Symbols, back, artist credits and templates saved as defaults for new decks.');});
@@ -94 +95 @@
-  const host=modal(f.name,`<div class="inspector"><div class="inspector-preview">${preview(c,f)?`<img src="${esc(preview(c,f))}" id="inspector-image" alt="${esc(f.name)}">`:'<div class="card-image">No artwork yet</div>'}${c.faces.length===2?`<button class="button quiet wide" id="inspect-flip">Edit ${index===0?'reverse':'front'} face ↻</button>`:''}<div class="subtitle-line">${esc(comp?.render?'CardConjurer render':'Artwork preview; generate images for the full card.')}</div>${comp?.crop?.warning?`<div class="notice">Crop warning: ${(comp.crop.cropX*100).toFixed(1)}% width / ${(comp.crop.cropY*100).toFixed(1)}% height outside the art window.</div>`:''}</div><div><h3>Card details</h3><div class="field-row"><label class="field"><span>Quantity in this deck</span><input id="card-qty" type="number" min="1" max="9999" step="1" value="${c.quantity}"></label><div class="field"><span>Selected printing</span><button class="button" id="choose-printing">${esc((c.scryfall.set||'').toUpperCase())} · ${esc(c.scryfall.collector_number||'')} &nbsp; Change ↗</button></div></div><label class="field"><span>Artist credit for this face</span><input id="face-artist" value="${esc(f.artistOverride??'')}" placeholder="${esc(d.settings.artist||sfFace.artist||c.scryfall.artist||'Use the deck default')}"><small>Leave blank to inherit. <label><input type="checkbox" id="blank-credit" ${f.artistOverride===''?'checked':''}> Intentionally show no credit</label></small></label><label class="field"><span>Template for this face</span><select id="face-template"><option value="">Use deck rule</option>${templateOptions(group,legendary,f.templateOverride||'auto')}</select><small>Layout: ${esc(state.bootstrap.groups[group]||group)}. Incompatible choices are hidden.</small></label>
+  const host=modal(f.name,`<div class="inspector"><div class="inspector-preview">${preview(c,f)?`<img src="${esc(preview(c,f))}" id="inspector-image" alt="${esc(f.name)}">`:'<div class="card-image">No artwork yet</div>'}${c.faces.length===2?`<button class="button quiet wide" id="inspect-flip">Edit ${index===0?'reverse':'front'} face ↻</button>`:''}<div class="subtitle-line">${esc(comp?.render?'CardConjurer render':'Artwork preview; generate images for the full card.')}</div>${comp?.crop?.warning?`<div class="notice">Crop warning: ${(comp.crop.cropX*100).toFixed(1)}% width / ${(comp.crop.cropY*100).toFixed(1)}% height outside the art window.</div>`:''}</div><div><h3>Card details</h3><div class="field-row"><label class="field"><span>Quantity in this deck</span><input id="card-qty" type="number" min="1" max="9999" step="1" value="${c.quantity}"></label><div class="field"><span>Selected printing</span><button class="button" id="choose-printing">${esc((c.scryfall.set||'').toUpperCase())} · ${esc(c.scryfall.collector_number||'')} &nbsp; Change ↗</button></div></div>${creditFields(d,c,f)}<label class="field"><span>Template for this face</span><select id="face-template"><option value="">Use deck rule</option>${templateOptions(group,legendary,f.templateOverride||'auto')}</select><small>Layout: ${esc(state.bootstrap.groups[group]||group)}. Incompatible choices are hidden.</small></label>
@@ -97,0 +99 @@
+  const creditControls=bindCreditFields(host,d,c,f,()=>artOverride);
@@ -100,2 +102,2 @@
-  $('#face-art').onclick=()=>attempt(async()=>{const file=await pickFile();if(!file)return;artOverride=(await uploadImage(file)).id;changedArt=true;fit={};$('#face-art-state').textContent='Custom art: '+file.name;if($('#inspector-image'))$('#inspector-image').src=asset(artOverride);});
-  $('#clear-face-art').onclick=()=>{artOverride=null;changedArt=true;fit={};$('#face-art-state').textContent='Using deck source after regeneration';};
+  $('#face-art').onclick=()=>attempt(async()=>{const file=await pickFile();if(!file)return;artOverride=(await uploadImage(file)).id;changedArt=true;fit={};$('#face-art-state').textContent='Custom art: '+file.name;if($('#inspector-image'))$('#inspector-image').src=asset(artOverride);creditControls.refresh();});
+  $('#clear-face-art').onclick=()=>{artOverride=null;changedArt=true;fit={};$('#face-art-state').textContent='Using deck source after regeneration';creditControls.refresh();};
@@ -110 +112 @@
-      const artist=$('#blank-credit').checked?'':$('#face-artist').value||null;
+      const credits=creditControls.values();
@@ -115 +117 @@
-      const changes={artistOverride:artist,artOverride:artOverride||null,templateOverride:$('#face-template').value||null,fit,semanticOverrides:semantic};
+      const changes={...credits,artOverride:artOverride||null,templateOverride:$('#face-template').value||null,fit,semanticOverrides:semantic};
diff --git a/site/settings.js b/site/settings.js
index dc0dc41..26fd0cd 100644
--- a/site/settings.js
+++ b/site/settings.js
@@ -8 +8 @@
-  <section class="panel section-gap"><div class="panel-head"><div><h2>Reusable deck style</h2><p>Save defaults from a deck’s More menu, then reuse its symbols, back, artist and template choices when importing another deck.</p></div></div><div class="actions"><span class="badge">${Object.keys(settings.defaults?.symbols||{}).length}/4 rarity symbols</span><span class="badge">${settings.defaults?.backAsset?'Back saved':'No default back'}</span><span class="badge">${esc(settings.defaults?.artist||'Printing artist')}</span><button class="button quiet small" id="clear-defaults">Clear saved style defaults</button></div></section>
+  <section class="panel section-gap"><div class="panel-head"><div><h2>Reusable deck style</h2><p>Save defaults from a deck’s More menu, then reuse its symbols, back, artist/modification credits and template choices when importing another deck.</p></div></div><div class="actions"><span class="badge">${Object.keys(settings.defaults?.symbols||{}).length}/4 rarity symbols</span><span class="badge">${settings.defaults?.backAsset?'Back saved':'No default back'}</span><span class="badge">${esc(settings.defaults?.artist||'Printing artist')}</span><span class="badge">${esc(settings.defaults?.modificationCredit||'No modification credit')}</span><button class="button quiet small" id="clear-defaults">Clear saved style defaults</button></div></section>
@@ -29 +29 @@
-  <section class="panel"><h2>What stays unchanged</h2><p class="muted">The approved Card Tools v48 compiler is included unchanged under <code>vendor/card_tools</code>. Built-in automatic layouts retain its frame layers, masks, text placement, station treatment, autofit and special card rules.</p><div class="notice">Not every unusual card layout has an approved built-in recipe. The app identifies structural differences and asks for a compatible custom template instead of silently printing the wrong layout.</div><h3>Local data and online access</h3><p class="muted">The app runs only on your computer’s loopback address. Scryfall, public GitHub images and the needed pinned CardConjurer assets require a connection on first use. The full CardConjurer repository is never downloaded.</p><p class="muted">The printer can change its editor. A paired ZIP download remains available even when browser automation needs an update.</p></section></div>
+  <section class="panel"><h2>What stays unchanged</h2><p class="muted">The approved Card Tools v54 compiler is included unchanged under <code>vendor/card_tools</code>. Built-in automatic layouts retain its frame layers, masks, text placement, station treatment, autofit and special card rules.</p><div class="notice">Not every unusual card layout has an approved built-in recipe. The app identifies structural differences and asks for a compatible custom template instead of silently printing the wrong layout.</div><h3>Local data and online access</h3><p class="muted">The app runs only on your computer’s loopback address. Scryfall, public GitHub images and the needed pinned CardConjurer assets require a connection on first use. The full CardConjurer repository is never downloaded.</p><p class="muted">The printer can change its editor. A paired ZIP download remains available even when browser automation needs an update.</p></section></div>
diff --git a/site/setup.js b/site/setup.js
index feaf821..4f2c89c 100644
--- a/site/setup.js
+++ b/site/setup.js
@@ -1,0 +2 @@
+import {originalArtist,composeCredit} from './credits.js';
@@ -29,2 +30,2 @@
-    <section class="panel"><div class="panel-head"><div><span class="eyebrow">05 / DETAILS</span><h2>Credit the artist</h2></div></div><label class="field"><span>Artist for the whole deck</span><input id="deck-artist" value="${esc(s.artist||'')}" placeholder="Use each selected printing’s original artist"><small>Leave blank to use the printing’s artist. Set an individual override from any card. Custom artwork should be credited to its actual creator.</small></label><label class="field"><span>Deck name</span><input id="deck-name" maxlength="200" value="${esc(deck.name)}"></label><label class="field"><span>Notes <small>only visible here</small></span><textarea id="deck-notes" rows="3">${esc(deck.notes||'')}</textarea></label>
-      <label class="field"><span>Reuse style from another deck</span><select id="reuse-style"><option value="">Choose a deck…</option>${state.decks.filter(d=>d.id!==deck.id).map(d=>`<option value="${d.id}">${esc(d.name)}</option>`).join('')}</select><small>Copies its symbols, back, artist and template choices—not its card list or artwork folder.</small></label>
+    <section class="panel"><div class="panel-head"><div><span class="eyebrow">05 / DETAILS</span><h2>Credit the artist</h2></div></div><label class="field"><span>Artist for custom artwork in this deck</span><input id="deck-artist" value="${esc(s.artist||'')}" placeholder="Use each selected printing’s original artist"><small>Used only for custom artwork. Scryfall originals and fallback images always keep the real printing artist. Leave blank to inherit the printing artist for custom art too.</small></label><label class="field"><span>Modification credit for this deck <small>optional</small></span><input id="deck-modification" maxlength="160" value="${esc(s.modificationCredit||'')}" placeholder="e.g. Modified by ChatGPT"><small>Appended after each card’s artist. You can change or suppress it per face.</small></label><div class="credit-preview" aria-live="polite"><span class="eyebrow">EXAMPLE ARTIST LINES</span><small>Scryfall artwork</small><strong id="scryfall-credit-preview"></strong><small>Custom artwork</small><strong id="custom-credit-preview"></strong></div><label class="field"><span>Deck name</span><input id="deck-name" maxlength="200" value="${esc(deck.name)}"></label><label class="field"><span>Notes <small>only visible here</small></span><textarea id="deck-notes" rows="3">${esc(deck.notes||'')}</textarea></label>
+      <label class="field"><span>Reuse style from another deck</span><select id="reuse-style"><option value="">Choose a deck…</option>${state.decks.filter(d=>d.id!==deck.id).map(d=>`<option value="${d.id}">${esc(d.name)}</option>`).join('')}</select><small>Copies its symbols, back, artist/modification credits and template choices—not its card list or artwork folder.</small></label>
@@ -36,0 +38,8 @@
+  function creditPreview(){
+    const sample=deck.cards[0],first=sample?.faces?.[0]||{};
+    const name=sample?originalArtist(sample,first)||'Original Artist':'Original Artist';
+    const artist=$('#deck-artist',root).value,suffix=$('#deck-modification',root).value;
+    $('#scryfall-credit-preview',root).textContent=composeCredit(name,null,artist,null,null,suffix,'scryfall');
+    $('#custom-credit-preview',root).textContent=composeCredit(name,null,artist,null,null,suffix,'custom');
+  }
+  $('#deck-artist',root).addEventListener('input',creditPreview);$('#deck-modification',root).addEventListener('input',creditPreview);creditPreview();
@@ -44 +53 @@
-  $('#reuse-style',root).onchange=async e=>attempt(async()=>{const id=e.target.value;if(!id)return;const other=await api('/api/decks/'+id);for(const key of ['symbols','backAsset','artist','templateRules'])s[key]=structuredClone(other.settings[key]|| (key==='symbols'||key==='templateRules'?{}:''));redrawSymbols();redrawBack();$('#deck-artist',root).value=s.artist;$$('[data-rule]',root).forEach(el=>el.innerHTML=templateOptions(el.dataset.rule,['legendary','legendary-land'].includes(el.dataset.rule),s.templateRules[el.dataset.rule]||'auto'));mark();});
+  $('#reuse-style',root).onchange=async e=>attempt(async()=>{const id=e.target.value;if(!id)return;const other=await api('/api/decks/'+id);for(const key of ['symbols','backAsset','artist','modificationCredit','templateRules'])s[key]=structuredClone(other.settings[key]|| (key==='symbols'||key==='templateRules'?{}:''));redrawSymbols();redrawBack();$('#deck-artist',root).value=s.artist;$('#deck-modification',root).value=s.modificationCredit||'';creditPreview();$$('[data-rule]',root).forEach(el=>el.innerHTML=templateOptions(el.dataset.rule,['legendary','legendary-land'].includes(el.dataset.rule),s.templateRules[el.dataset.rule]||'auto'));mark();});
@@ -47 +56 @@
-    s.useLandLibrary=$('#use-land-library',root).checked;s.landLibrary=$('#land-library',root).value.trim();s.artist=$('#deck-artist',root).value;
+    s.useLandLibrary=$('#use-land-library',root).checked;s.landLibrary=$('#land-library',root).value.trim();s.artist=$('#deck-artist',root).value;s.modificationCredit=$('#deck-modification',root).value.trim();
diff --git a/site/styles.css b/site/styles.css
index b26c8e1..b41a737 100644
--- a/site/styles.css
+++ b/site/styles.css
@@ -18,0 +19,2 @@
+
+.credit-fields{margin:12px 0 24px}.credit-original{border:1px solid var(--line);border-radius:7px;background:#14191f;padding:10px 12px;font-size:13px;overflow-wrap:anywhere}.credit-preview{padding:13px 16px;border:1px solid #4c4438;background:#211e19;border-radius:9px;margin:12px 0 20px}.credit-preview strong{display:block;overflow-wrap:anywhere;font-weight:550;font-size:13px}.credit-preview small{display:block;font-size:10px;line-height:1.6;margin-top:7px}.card-credit{font-size:9px;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;margin:0 1px 4px}.credit-fields input:disabled{opacity:.58;cursor:not-allowed}
diff --git a/tests/test_dom_offline.py b/tests/test_dom_offline.py
index f51257e..6844198 100644
--- a/tests/test_dom_offline.py
+++ b/tests/test_dom_offline.py
@@ -14 +14 @@
-    for name in ['ui','setup','render','orders','templates','settings','deck','app']:
+    for name in ['ui','credits','setup','render','orders','templates','settings','deck','app']:
